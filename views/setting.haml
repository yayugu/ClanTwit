%h2
  %span.small
    クラン
  = h session[:clan_name]

%a{:href => "/delete_member/#{session[:screen_name]}"}  
  このクランへの参加をやめる

.block
  - p $db[session[:clan_name]]
  - p $db[session[:clan_name]][session[:screen_name]]
  - p $db[session[:clan_name]][session[:screen_name]][:owner]
  - if $db[session[:clan_name]][session[:screen_name]][:owner]
    メンバーの管理
    - $db[session[:clan_name]].each_value do |member|
      %div
        %img{:src => "http://api.dan.co.jp/twicon/#{h member[:screen_name]}/normal"}
        %a{:href => "http://twitter.com/#{h member[:screen_name]}"}
          @#{h member[:screen_name]}
        - if member[:owner]
          オーナー！
        %a{:href => "/delete_member/#{member[:screen_name]}"}
          このメンバーをクランから外す

  
